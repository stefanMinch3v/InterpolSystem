namespace InterpolSystem.Api
{
    public class WebConstants
    {
        public const string PageNumber = "index/{page}";
        public const string UnexistingPage = "Page does not exist.";

        public const string PersonDetails = "details/{id}";
        public const string UnexistingPerson = "The person that you looking for does not exist.";

        public const string ArticleDetails = "details/{id}";
    }
}
